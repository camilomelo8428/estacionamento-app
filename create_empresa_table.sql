CREATE TABLE IF NOT EXISTS public.empresa (id UUID DEFAULT gen_random_uuid() PRIMARY KEY, nome VARCHAR NOT NULL, telefone VARCHAR, endereco VARCHAR, mensagem VARCHAR, created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL, updated_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL); ALTER TABLE public.empresa DISABLE ROW LEVEL SECURITY; GRANT ALL ON public.empresa TO authenticated; GRANT ALL ON public.empresa TO anon;
